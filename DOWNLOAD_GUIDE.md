# دليل تحميل وتثبيت نظام المحاسبة والمراجعة Web3

## 📥 كيفية تحميل المشروع على جهازك المحلي

### المتطلبات الأساسية

قبل البدء، تأكد من تثبيت البرامج التالية على جهازك:

- **Python 3.11 أو أحدث** - [تحميل من python.org](https://python.org)
- **Node.js 18+** (اختياري، للواجهة الأمامية) - [تحميل من nodejs.org](https://nodejs.org)
- **Git** - لاستنساخ المستودع
- **محفظة MetaMask** - [تحميل من metamask.io](https://metamask.io/download/)

### خطوات التحميل والتثبيت

#### 1. استنساخ المستودع

```bash
# افتح الطرفية (Terminal/Command Prompt)
git clone https://github.com/yourusername/web3-accounting-system.git
cd web3-accounting-system
```

#### 2. تثبيت التبعيات

```bash
# تثبيت تبعيات Python
pip install -r requirements.txt

# إذا كنت تريد تشغيل الواجهة الأمامية (اختياري)
cd frontend
npm install
cd ..
```

#### 3. تشغيل النظام

```bash
# تشغيل الخادم الرئيسي
python server/api.py

# في طرفية منفصلة، تشغيل الواجهة الأمامية (إذا كنت قد ثبتها)
cd frontend
npm run dev
```

### 🌐 الوصول إلى النظام

بعد التشغيل، افتح هذه الروابط في متصفحك:

- **الواجهة الرئيسية:** http://localhost:3000
- **توثيق API:** http://localhost:8000/api/docs
- **توثيق ReDoc:** http://localhost:8000/api/redoc

### 🔑 ربط محفظتك

1. ثبت إضافة MetaMask في متصفحك
2. أنشئ أو استورد محفظة
3. اذهب إلى http://localhost:3000 واضغط "Connect Wallet"

### 📋 متطلبات النظام

- **نظام التشغيل:** Windows 10+, macOS 10.15+, Linux (Ubuntu 18.04+)
- **الذاكرة:** 4GB RAM على الأقل
- **التخزين:** 1GB مساحة فارغة
- **الاتصال بالإنترنت:** مطلوب لتحميل التبعيات

### 🛠️ استكشاف الأخطاء

#### إذا ظهر خطأ "Python not found":
```bash
# تأكد من تثبيت Python وإضافته للمسار
python --version
pip --version
```

#### إذا كانت المنافذ مشغولة:
```bash
# أوقف العمليات على المنافذ 8000 و 3000
# على Windows:
netstat -ano | findstr :8000
taskkill /PID <PID> /F

# على Linux/macOS:
lsof -ti:8000,3000 | xargs kill -9
```

#### إذا لم تتصل MetaMask:
- تأكد من أنك على localhost أو موقع HTTPS
- امسح ذاكرة التخزين المؤقت للمتصفح
- أعد تحميل صفحة MetaMask

### ✅ التحقق من التثبيت

اختبر أن كل شيء يعمل بشكل صحيح:

```bash
# اختب الخادم
curl http://localhost:8000/api/health
```

يجب أن ترى رد JSON مع حالة النظام.

### 🔄 تحديث المشروع

للحصول على آخر التحديثات:

```bash
# في مجلد المشروع
git pull origin main

# إعادة تثبيت التبعيات إذا تغيرت
pip install -r requirements.txt
```

### 📱 التطبيق المحمول (اختياري)

إذا كنت تريد تشغيل التطبيق المحمول:

```bash
cd audit-mobile
npm install
npm start
```

### 📊 اختبار النظام

```bash
# تشغيل جميع الاختبارات
pytest

# تشغيل اختبار محدد
pytest core/tests/test_blockchain.py
```

### 📞 الدعم والمساعدة

- **التوثيق الكامل:** [docs/](docs/)
- **المشاكل:** [GitHub Issues](https://github.com/yourusername/web3-accounting-system/issues)
- **المناقشات:** [GitHub Discussions](https://github.com/yourusername/web3-accounting-system/discussions)

### ⚠️ ملاحظات مهمة

1. **المصادقة:** النظام يستخدم مصادقة المحفظة فقط (لا كلمات مرور)
2. **السجلات:** جميع السجلات غير قابلة للتغيير - صمم المعاملات بحرص
3. **وحدة المراجعة:** لا يمكن تعطيلها - جميع الإجراءات مسجلة بشكل دائم
4. **العقود الذكية:** غير قابلة للتغيير بعد النشر - اختبر جيداً قبل الإنتاج

---

**تم بناؤه بـ ❤️ لمستقبل المحاسبة اللامركزية**

*جعل الأنظمة المالية شفافة وغير قابلة للتغيير وموثوقة.*